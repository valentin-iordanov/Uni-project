# Gracia House Frontend

This directory contains a small React application demonstrating various components for a guest house website. To run the app locally, install dependencies and start the development server:

```bash
npm install
npm start
```

The application uses React 18 and `react-icons`. Due to environment restrictions, dependencies are not installed by default.
